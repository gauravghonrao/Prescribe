server:
  port: 8088
    
spring:
  application:
    name: CONFIGURATION-SERVER
  cloud:
    config:
       server:
        git:
          uri: https://github.com/gauravghonrao/spring-cloud-config
          clone-on-start: true  
  profiles:
    active: native